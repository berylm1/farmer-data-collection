FROM alpine:3.19
RUN apk --no-cache add socat
EXPOSE 3000
HEALTHCHECK --interval=10s --timeout=3s --retries=3 \
  CMD nc -z localhost 3000 || exit 1
CMD ["socat", "TCP-LISTEN:3000,reuseaddr,fork", "EXEC:/bin/true"]
