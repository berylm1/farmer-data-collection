FROM alpine:3.19
RUN apk --no-cache add socat
EXPOSE 9003
HEALTHCHECK --interval=10s --timeout=3s --retries=3 \
  CMD nc -z localhost 9003 || exit 1
CMD ["socat", "TCP-LISTEN:9003,reuseaddr,fork", "EXEC:/bin/true"]
